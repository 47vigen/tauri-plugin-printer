{
  "name": "tauri-plugin-printer-api",
  "version": "4.2.0",
  "author": "47vigen",
  "type": "commonjs",
  "description": "Tauri Printer API",
  "browser": "webview-dist/index.js",
  "main": "webview-dist/index.js",
  "types": "webview-dist/index.d.ts",
  "scripts": {
    "build": "cd ./webview-src && tsc -b --force",
    "prepublishOnly": "yarn build",
    "pretest": "yarn build"
  },
  "devDependencies": {
    "@types/node": "^20.12.8",
    "typescript": "4.7.3"
  },
  "dependencies": {
    "@tauri-apps/api": ">=2.0.0-beta.0",
    "buffer": "^6.0.3",
    "nanoid": "^5.0.7",
    "tslib": "^2.1.0"
  }
}
